# v0.49.37 - IMPLEMENTS clause parsing + method lifting
SPECIES Dog IMPLEMENTS Speakable {
  name: TX
  ACTION speak() -> TX {
    GIVE self.name + " barks!".
  }
}
ACTION main(),
  BLOOM Dog AS d.
  CALL d.put("name", "Rex").
  SHOW d.speak().
  GIVE 0.
/GIVE main.
