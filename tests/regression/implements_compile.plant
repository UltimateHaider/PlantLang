SPECIES Dog {
  name: TX
  ACTION speak() -> TX {
    GIVE self.name + " barks!".
  }
}
ACTION main(),
  CALL plant_impl_iface("Dog", "Speakable").
  BLOOM Dog AS d.
  CALL d.put("name", "Rex").
  SHOW d.speak().
  REAP chk FROM plant_is_a, d, "Speakable".
  SHOW chk.
  GIVE 0.
/GIVE main.
