# REF T pass-by-reference params under monomorphization
ACTION idref[T](a(REF T), b(T)),
  GIVE b.
/ACTION.

ACTION main(),
  CREATE x(NUM) TO 5.
  REAP r FROM idref[NUM], x, 7.
  REAP rs FROM _from_long, r.
  SHOW "idref=" + rs.
  GIVE 0.
/GIVE main.
