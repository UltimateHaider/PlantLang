# ═══════════════════════════════════════════════════════════════
#  src/plantc/lexer.plant — Lexical analyzer for the
#  self-hosting PlantLang compiler.
# ═══════════════════════════════════════════════════════════════

# ── is_keyword(word) ──────────────────────────────────────
ACTION is_keyword(wrd(TX)),
  IF wrd IS "LET", GIVE TRUE. /IF.
  IF wrd IS "CREATE", GIVE TRUE. /IF.
  IF wrd IS "MATCH", GIVE TRUE. /IF.
  IF wrd IS "IF", GIVE TRUE. /IF.
  IF wrd IS "ELSE", GIVE TRUE. /IF.
  IF wrd IS "LOOP", GIVE TRUE. /IF.
  IF wrd IS "RETURN", GIVE TRUE. /IF.
  IF wrd IS "TYPE", GIVE TRUE. /IF.
  IF wrd IS "SHOW", GIVE TRUE. /IF.
  IF wrd IS "ACTION", GIVE TRUE. /IF.
  IF wrd IS "REAP", GIVE TRUE. /IF.
  IF wrd IS "GIVE", GIVE TRUE. /IF.
  IF wrd IS "SET", GIVE TRUE. /IF.
  IF wrd IS "PUT", GIVE TRUE. /IF.
  IF wrd IS "TAKE", GIVE TRUE. /IF.
  IF wrd IS "FOR", GIVE TRUE. /IF.
  IF wrd IS "CYCLE", GIVE TRUE. /IF.
  IF wrd IS "SEASON", GIVE TRUE. /IF.
  IF wrd IS "WEATHER", GIVE TRUE. /IF.
  IF wrd IS "SHELTER", GIVE TRUE. /IF.
  IF wrd IS "CALM", GIVE TRUE. /IF.
  IF wrd IS "AND", GIVE TRUE. /IF.
  IF wrd IS "OR", GIVE TRUE. /IF.
  IF wrd IS "verify", GIVE TRUE. /IF.
  IF wrd IS "suite", GIVE TRUE. /IF.
  IF wrd IS "NOT", GIVE TRUE. /IF.
  IF wrd IS "IS", GIVE TRUE. /IF.
  IF wrd IS "TRUE", GIVE TRUE. /IF.
  IF wrd IS "FALSE", GIVE TRUE. /IF.
  IF wrd IS "TO", GIVE TRUE. /IF.
  IF wrd IS "FROM", GIVE TRUE. /IF.
  IF wrd IS "AS", GIVE TRUE. /IF.
  IF wrd IS "IN", GIVE TRUE. /IF.
  IF wrd IS "SPLIT", GIVE TRUE. /IF.
  IF wrd IS "JOIN", GIVE TRUE. /IF.
  # v0.49.13 — bare string built-ins.
  IF wrd IS "INCLUDES", GIVE TRUE. /IF.
  IF wrd IS "STARTS_WITH", GIVE TRUE. /IF.
  IF wrd IS "ENDS_WITH", GIVE TRUE. /IF.
  IF wrd IS "REPEAT", GIVE TRUE. /IF.
  IF wrd IS "PAD", GIVE TRUE. /IF.
  IF wrd IS "PAD_LEFT", GIVE TRUE. /IF.
  # v0.49.15 — list built-ins (batch 1).
  IF wrd IS "REVERSE", GIVE TRUE. /IF.
  IF wrd IS "RANGE", GIVE TRUE. /IF.
  IF wrd IS "SORT", GIVE TRUE. /IF.
  IF wrd IS "INDEX_OF", GIVE TRUE. /IF.
  IF wrd IS "UNIQUE", GIVE TRUE. /IF.
  IF wrd IS "AVERAGE", GIVE TRUE. /IF.
  IF wrd IS "MEDIAN", GIVE TRUE. /IF.
  # v0.49.16 — list built-ins (batch 2).
  IF wrd IS "FLATTEN", GIVE TRUE. /IF.
  IF wrd IS "CHUNK", GIVE TRUE. /IF.
  IF wrd IS "ZIP", GIVE TRUE. /IF.
  IF wrd IS "FILTER_GT", GIVE TRUE. /IF.
  IF wrd IS "FILTER_LT", GIVE TRUE. /IF.
  IF wrd IS "ENUM", GIVE TRUE. /IF.
  IF wrd IS "CONST", GIVE TRUE. /IF.
  IF wrd IS "ROOT", GIVE TRUE. /IF.
  IF wrd IS "ROOT_SCOPE", GIVE TRUE. /IF.
  IF wrd IS "FUNCTION", GIVE TRUE. /IF.
  IF wrd IS "IMPORT", GIVE TRUE. /IF.
  IF wrd IS "OPTION", GIVE TRUE. /IF.
  IF wrd IS "RESULT", GIVE TRUE. /IF.
  IF wrd IS "GREATER", GIVE TRUE. /IF.
  IF wrd IS "LESS", GIVE TRUE. /IF.
  IF wrd IS "THAN", GIVE TRUE. /IF.
  IF wrd IS "COUNT", GIVE TRUE. /IF.
  IF wrd IS "NOW", GIVE TRUE. /IF.
  IF wrd IS "ANALYZE", GIVE TRUE. /IF.
  IF wrd IS "TYPEOF", GIVE TRUE. /IF.
  IF wrd IS "FIRST", GIVE TRUE. /IF.
  IF wrd IS "LAST", GIVE TRUE. /IF.
  IF wrd IS "SUM", GIVE TRUE. /IF.
  IF wrd IS "NULL", GIVE TRUE. /IF.
  IF wrd IS "ISNT", GIVE TRUE. /IF.
  IF wrd IS "TEST", GIVE TRUE. /IF.
  IF wrd IS "BREAK", GIVE TRUE. /IF.
  IF wrd IS "CONTINUE", GIVE TRUE. /IF.
  IF wrd IS "THROW", GIVE TRUE. /IF.
  IF wrd IS "STOP", GIVE TRUE. /IF.
  IF wrd IS "STEP", GIVE TRUE. /IF.
  IF wrd IS "INTO", GIVE TRUE. /IF.
  IF wrd IS "WAIT", GIVE TRUE. /IF.
  IF wrd IS "LOCK", GIVE TRUE. /IF.
  GIVE FALSE.
/ACTION.

# ── keyword_to_type(word) ─────────────────────────────────
ACTION keyword_to_type(wrd(TX)),
  IF wrd IS "LET", GIVE "LET". /IF.
  IF wrd IS "CREATE", GIVE "CREATE". /IF.
  IF wrd IS "MATCH", GIVE "MATCH". /IF.
  IF wrd IS "IF", GIVE "IF". /IF.
  IF wrd IS "ELSE", GIVE "ELSE". /IF.
  IF wrd IS "LOOP", GIVE "LOOP". /IF.
  IF wrd IS "RETURN", GIVE "RETURN". /IF.
  IF wrd IS "TYPE", GIVE "TYPE". /IF.
  IF wrd IS "SHOW", GIVE "SHOW". /IF.
  IF wrd IS "ACTION", GIVE "ACTION". /IF.
  IF wrd IS "REAP", GIVE "REAP". /IF.
  IF wrd IS "GIVE", GIVE "GIVE". /IF.
  IF wrd IS "SET", GIVE "SET". /IF.
  IF wrd IS "PUT", GIVE "PUT". /IF.
  IF wrd IS "TAKE", GIVE "TAKE". /IF.
  IF wrd IS "FOR", GIVE "FOR". /IF.
  IF wrd IS "CYCLE", GIVE "CYCLE". /IF.
  IF wrd IS "SEASON", GIVE "SEASON". /IF.
  IF wrd IS "WEATHER", GIVE "WEATHER". /IF.
  IF wrd IS "SHELTER", GIVE "SHELTER". /IF.
  IF wrd IS "CALM", GIVE "CALM". /IF.
  IF wrd IS "AND", GIVE "AND". /IF.
  IF wrd IS "OR", GIVE "OR". /IF.
  IF wrd IS "NOT", GIVE "NOT". /IF.
  IF wrd IS "IS", GIVE "IS". /IF.
  IF wrd IS "TRUE", GIVE "TRUE". /IF.
  IF wrd IS "FALSE", GIVE "FALSE". /IF.
  IF wrd IS "TO", GIVE "TO". /IF.
  IF wrd IS "FROM", GIVE "FROM". /IF.
  IF wrd IS "AS", GIVE "AS". /IF.
  IF wrd IS "IN", GIVE "IN". /IF.
  IF wrd IS "SPLIT", GIVE "SPLIT". /IF.
  IF wrd IS "JOIN", GIVE "JOIN". /IF.
  # v0.49.13 — bare string built-ins.
  IF wrd IS "INCLUDES", GIVE "INCLUDES". /IF.
  IF wrd IS "STARTS_WITH", GIVE "STARTS_WITH". /IF.
  IF wrd IS "ENDS_WITH", GIVE "ENDS_WITH". /IF.
  IF wrd IS "REPEAT", GIVE "REPEAT". /IF.
  IF wrd IS "PAD", GIVE "PAD". /IF.
  IF wrd IS "PAD_LEFT", GIVE "PAD_LEFT". /IF.
  # v0.49.15 — list built-ins (batch 1).
  IF wrd IS "REVERSE", GIVE "REVERSE". /IF.
  IF wrd IS "RANGE", GIVE "RANGE". /IF.
  IF wrd IS "SORT", GIVE "SORT". /IF.
  IF wrd IS "INDEX_OF", GIVE "INDEX_OF". /IF.
  IF wrd IS "UNIQUE", GIVE "UNIQUE". /IF.
  IF wrd IS "AVERAGE", GIVE "AVERAGE". /IF.
  IF wrd IS "MEDIAN", GIVE "MEDIAN". /IF.
  # v0.49.16 — list built-ins (batch 2).
  IF wrd IS "FLATTEN", GIVE "FLATTEN". /IF.
  IF wrd IS "CHUNK", GIVE "CHUNK". /IF.
  IF wrd IS "ZIP", GIVE "ZIP". /IF.
  IF wrd IS "FILTER_GT", GIVE "FILTER_GT". /IF.
  IF wrd IS "FILTER_LT", GIVE "FILTER_LT". /IF.
  # v0.49.17 — extended math built-ins.
  IF wrd IS "MIN", GIVE "MIN". /IF.
  IF wrd IS "MAX", GIVE "MAX". /IF.
  IF wrd IS "TAN", GIVE "TAN". /IF.
  IF wrd IS "ATAN", GIVE "ATAN". /IF.
  IF wrd IS "COT", GIVE "COT". /IF.
  IF wrd IS "ASIN", GIVE "ASIN". /IF.
  IF wrd IS "ACOS", GIVE "ACOS". /IF.
  IF wrd IS "ATAN2", GIVE "ATAN2". /IF.
  IF wrd IS "SINH", GIVE "SINH". /IF.
  IF wrd IS "COSH", GIVE "COSH". /IF.
  IF wrd IS "TANH", GIVE "TANH". /IF.
  IF wrd IS "EXP", GIVE "EXP". /IF.
  IF wrd IS "EXPM1", GIVE "EXPM1". /IF.
  # v0.49.33 - interfaces.
  IF wrd IS "INTERFACE", GIVE "INTERFACE". /IF.
  IF wrd IS "IMPLEMENTS", GIVE "IMPLEMENTS". /IF.
  # v0.49.28 - SPECIES/BLOOM object model.
  IF wrd IS "SPECIES", GIVE "SPECIES". /IF.
  IF wrd IS "BLOOM", GIVE "BLOOM". /IF.
  # v0.49.23 — numerical analysis built-ins.
  IF wrd IS "LU", GIVE "LU". /IF.
  IF wrd IS "EIGEN", GIVE "EIGEN". /IF.
  IF wrd IS "SVD", GIVE "SVD". /IF.
  IF wrd IS "SOLVE", GIVE "SOLVE". /IF.
  IF wrd IS "COND", GIVE "COND". /IF.
  # v0.49.22 — linear algebra built-ins.
  IF wrd IS "DOT", GIVE "DOT". /IF.
  IF wrd IS "CROSS", GIVE "CROSS". /IF.
  IF wrd IS "NORM", GIVE "NORM". /IF.
  IF wrd IS "TRANSPOSE", GIVE "TRANSPOSE". /IF.
  IF wrd IS "MATRIX_MULT", GIVE "MATRIX_MULT". /IF.
  IF wrd IS "INVERSE", GIVE "INVERSE". /IF.
  IF wrd IS "DET", GIVE "DET". /IF.
  # v0.49.21 — statistical & array aggregation built-ins.
  IF wrd IS "VARIANCE", GIVE "VARIANCE". /IF.
  IF wrd IS "STDDEV", GIVE "STDDEV". /IF.
  IF wrd IS "PRODUCT", GIVE "PRODUCT". /IF.
  IF wrd IS "MODE", GIVE "MODE". /IF.
  # v0.49.19 — natural logarithm built-in.
  IF wrd IS "LOG", GIVE "LOG". /IF.
  IF wrd IS "LOG10", GIVE "LOG10". /IF.
  IF wrd IS "LOG2", GIVE "LOG2". /IF.
  IF wrd IS "LOG1P", GIVE "LOG1P". /IF.
  IF wrd IS "HYPOT", GIVE "HYPOT". /IF.
  # v0.49.18 — advanced math built-ins.
  IF wrd IS "SEC", GIVE "SEC". /IF.
  IF wrd IS "CSC", GIVE "CSC". /IF.
  IF wrd IS "ASINH", GIVE "ASINH". /IF.
  IF wrd IS "ACOSH", GIVE "ACOSH". /IF.
  IF wrd IS "ATANH", GIVE "ATANH". /IF.
  IF wrd IS "ERF", GIVE "ERF". /IF.
  IF wrd IS "ERFC", GIVE "ERFC". /IF.
  IF wrd IS "GAMMA", GIVE "GAMMA". /IF.
  IF wrd IS "LGAMMA", GIVE "LGAMMA". /IF.
  IF wrd IS "EXP2", GIVE "EXP2". /IF.
  IF wrd IS "LOG_BASE", GIVE "LOG_BASE". /IF.
  IF wrd IS "ENUM", GIVE "ENUM". /IF.
  IF wrd IS "CONST", GIVE "CONST". /IF.
  IF wrd IS "ROOT", GIVE "ROOT". /IF.
  IF wrd IS "ROOT_SCOPE", GIVE "ROOT_SCOPE". /IF.
  IF wrd IS "FUNCTION", GIVE "FUNCTION". /IF.
  IF wrd IS "IMPORT", GIVE "IMPORT". /IF.
  IF wrd IS "OPTION", GIVE "OPTION". /IF.
  IF wrd IS "RESULT", GIVE "RESULT". /IF.
  IF wrd IS "GREATER", GIVE "GREATER". /IF.
  IF wrd IS "LESS", GIVE "LESS". /IF.
  IF wrd IS "THAN", GIVE "THAN". /IF.
  IF wrd IS "COUNT", GIVE "COUNT". /IF.
  IF wrd IS "NOW", GIVE "NOW". /IF.
  IF wrd IS "ANALYZE", GIVE "ANALYZE". /IF.
  IF wrd IS "TYPEOF", GIVE "TYPEOF". /IF.
  IF wrd IS "FIRST", GIVE "FIRST". /IF.
  IF wrd IS "LAST", GIVE "LAST". /IF.
  IF wrd IS "SUM", GIVE "SUM". /IF.
  IF wrd IS "NULL", GIVE "NULL". /IF.
  IF wrd IS "ISNT", GIVE "ISNT". /IF.
  IF wrd IS "TEST", GIVE "TEST". /IF.
  IF wrd IS "BREAK", GIVE "BREAK". /IF.
  IF wrd IS "CONTINUE", GIVE "CONTINUE". /IF.
  IF wrd IS "THROW", GIVE "THROW". /IF.
  IF wrd IS "STOP", GIVE "STOP". /IF.
  IF wrd IS "STEP", GIVE "STEP". /IF.
  IF wrd IS "INTO", GIVE "INTO". /IF.
  IF wrd IS "WAIT", GIVE "WAIT". /IF.
  IF wrd IS "LOCK", GIVE "LOCK". /IF.
  GIVE "IDENT".
/ACTION.

# ── char_type(ch) ─────────────────────────────────────────
ACTION char_type(ch(TX)),
  IF ch IS "+", GIVE "PLUS". /IF.
  IF ch IS "-", GIVE "MINUS". /IF.
  IF ch IS "*", GIVE "STAR". /IF.
  IF ch IS "/", GIVE "SLASH". /IF.
  IF ch IS "%", GIVE "PERCENT". /IF.
  IF ch IS "=", GIVE "EQUAL". /IF.
  IF ch IS ":", GIVE "COLON". /IF.
  IF ch IS "<", GIVE "LESS". /IF.
  IF ch IS ">", GIVE "GREATER". /IF.
  IF ch IS "!", GIVE "BANG". /IF.
  IF ch IS "?", GIVE "QUESTION". /IF.
  IF ch IS "&", GIVE "AMPERSAND". /IF.
  IF ch IS "|", GIVE "PIPE". /IF.
  IF ch IS ";", GIVE "SEMI". /IF.
  IF ch IS "(", GIVE "LPAREN". /IF.
  IF ch IS ")", GIVE "RPAREN". /IF.
  IF ch IS "{", GIVE "LBRACE". /IF.
  IF ch IS "}", GIVE "RBRACE". /IF.
  IF ch IS "[", GIVE "LBRACKET". /IF.
  IF ch IS "]", GIVE "RBRACKET". /IF.
  IF ch IS ",", GIVE "COMMA". /IF.
  IF ch IS ".", GIVE "DOT". /IF.
  IF ch IS "_", GIVE "WILDCARD". /IF.
  IF ch IS "#", GIVE "HASH". /IF.
  GIVE "".
/ACTION.

# ── is_alnum(ch) ──────────────────────────────────────────
ACTION is_alnum(ch(TX)),
  IF ch GREATER THAN OR EQUAL "a" AND ch LESS THAN OR EQUAL "z", GIVE TRUE. /IF.
  IF ch GREATER THAN OR EQUAL "A" AND ch LESS THAN OR EQUAL "Z", GIVE TRUE. /IF.
  IF ch GREATER THAN OR EQUAL "0" AND ch LESS THAN OR EQUAL "9", GIVE TRUE. /IF.
  IF ch IS "_", GIVE TRUE. /IF.
  GIVE FALSE.
/ACTION.

# ── is_alpha_start(ch) ────────────────────────────────────
ACTION is_alpha_start(ch(TX)),
  IF ch GREATER THAN OR EQUAL "a" AND ch LESS THAN OR EQUAL "z", GIVE TRUE. /IF.
  IF ch GREATER THAN OR EQUAL "A" AND ch LESS THAN OR EQUAL "Z", GIVE TRUE. /IF.
  IF ch IS "_", GIVE TRUE. /IF.
  GIVE FALSE.
/ACTION.

# ── match_ident_or_keyword(src, i, n) ─────────────────────
ACTION match_ident_or_keyword(src(TX), i(NUM), n(NUM)),
  CREATE wd(TX) TO "".
  CREATE ni(NUM) TO i.
  CREATE ch(TX) TO "".
  SEASON ni < n,
    REAP ok FROM is_alnum, char_at(src, ni).
    IF NOT ok, BREAK. /IF.
    REAP ch FROM char_at, src, ni.
    SET wd TO wd + ch.
    SET ni TO ni + 1.
  /SEASON.
  IF wd IS "null", SET wd TO "NULL". /IF.
  GIVE plant_list_make(2, wd, ni).
/ACTION.

# ── match_number(src, i, n) ───────────────────────────────
ACTION match_number(src(TX), i(NUM), n(NUM)),
  CREATE num(TX) TO "".
  CREATE ni(NUM) TO i.
  CREATE ch(TX) TO "".
  SEASON ni < n AND char_at(src, ni) GREATER THAN OR EQUAL "0" AND char_at(src, ni) LESS THAN OR EQUAL "9",
    REAP ch FROM char_at, src, ni.
    SET num TO num + ch.
    SET ni TO ni + 1.
  /SEASON.
  # v0.49.6 — decimal fraction: consume "." only when a digit
  # follows, so 1.5 is one NUMBER token while "SHOW x." still
  # terminates at the statement dot.
  IF ni + 1 < n AND char_at(src, ni) IS "." AND char_at(src, ni + 1) GREATER THAN OR EQUAL "0" AND char_at(src, ni + 1) LESS THAN OR EQUAL "9",
    SET num TO num + ".".
    SET ni TO ni + 1.
    SEASON ni < n AND char_at(src, ni) GREATER THAN OR EQUAL "0" AND char_at(src, ni) LESS THAN OR EQUAL "9",
      REAP ch FROM char_at, src, ni.
      SET num TO num + ch.
      SET ni TO ni + 1.
    /SEASON.
  /IF.
  GIVE plant_list_make(2, num, ni).
/ACTION.

# ── match_string(src, i, n) ───────────────────────────────
ACTION match_string(src(TX), i(NUM), n(NUM)),
  CREATE val(TX) TO "".
  CREATE si(NUM) TO i.
  CREATE done(FACT) TO FALSE.
  CREATE ch(TX) TO "".
  # v0.49.20 — the opening character selects the delimiter: " or '
  CREATE q(TX) TO "".
  IF si < n,
    REAP cq FROM char_at, src, si.
    IF cq IS "\"", SET q TO "\"". /IF.
    IF cq IS "'", SET q TO "'". /IF.
    IF q ISNT "", SET si TO si + 1. /IF.
  /IF.
  SEASON NOT done AND si < n,
    REAP ch FROM char_at, src, si.
    # str_eq: variable-vs-variable 'IS' lowers to pointer ==; only
    # literal comparisons compile to strcmp.
    REAP isq FROM str_eq, ch, q.
    IF isq IS "1", SET done TO TRUE. /IF.
    IF isq IS "1", CONTINUE. /IF.
    IF ch IS "\\",
      SET si TO si + 1.
      IF si < n,
        REAP ch FROM char_at, src, si.
        IF ch IS "n", SET ch TO "\n". /IF.
        IF ch IS "t", SET ch TO "\t". /IF.
        IF ch IS "r", SET ch TO "\r". /IF.
      /IF.
    /IF.
    IF si < n,
      SET val TO val + ch.
      SET si TO si + 1.
    /IF.
  /SEASON.
  IF si < n,
    REAP cend FROM char_at, src, si.
    REAP isq2 FROM str_eq, cend, q.
    IF isq2 IS "1", SET si TO si + 1. /IF.
  /IF.
  GIVE plant_list_make(2, val, si).
/ACTION.

# match_string_i: scan "..." with ${...} interpolation awareness
# (v0.48.22-patch2). `${` opens an expression region that ends at the
# matching `}`; nested `${` increments the depth and quoted strings
# inside the region do not close it. Returns [value, endpos, hasi].
# Escape rules match match_string (\n \t \r), applied only outside
# expression regions so embedded expressions stay raw.
ACTION match_string_i(src(TX), i(NUM), n(NUM)),
  CREATE val(TX) TO "".
  CREATE si(NUM) TO i.
  CREATE done(FACT) TO FALSE.
  CREATE ch(TX) TO "".
  CREATE idepth(NUM) TO 0.
  CREATE instr(NUM) TO 0.
  CREATE hasi(NUM) TO 0.
  IF si < n AND char_at(src, si) IS "\"", SET si TO si + 1. /IF.
  SEASON NOT done AND si < n,
    REAP ch FROM char_at, src, si.
    IF idepth IS 0 AND instr IS 0 AND ch IS "\"",
      SET done TO TRUE.
      CONTINUE.
    /IF.
    IF idepth IS 0 AND instr IS 0 AND ch IS "$" AND si + 1 < n AND char_at(src, si + 1) IS "{",
      SET val TO val + "$" + "{".
      SET idepth TO 1.
      SET hasi TO 1.
      SET si TO si + 2.
      CONTINUE.
    /IF.
    # v0.48.22-patch4 — escaped interpolation marker: "\${" stays
    # literal (backslash preserved, no ${...} region opened). The
    # generic escape branch below would otherwise drop the backslash
    # and treat ${ as an interpolation start.
    IF idepth IS 0 AND instr IS 0 AND ch IS "\\" AND si + 1 < n AND char_at(src, si + 1) IS "$" AND si + 2 < n AND char_at(src, si + 2) IS "{",
      SET val TO val + "\\" + "$" + "{".
      SET si TO si + 3.
      CONTINUE.
    /IF.
    IF idepth IS 0 AND instr IS 0 AND ch IS "\\",
      SET si TO si + 1.
      IF si < n,
        REAP ch FROM char_at, src, si.
        IF ch IS "n", SET ch TO "\n". /IF.
        IF ch IS "t", SET ch TO "\t". /IF.
        IF ch IS "r", SET ch TO "\r". /IF.
      /IF.
    /IF.
    IF idepth > 0 AND instr IS 0 AND ch IS "$" AND si + 1 < n AND char_at(src, si + 1) IS "{",
      SET val TO val + "$" + "{".
      SET idepth TO idepth + 1.
      SET si TO si + 2.
      CONTINUE.
    /IF.
    IF idepth > 0 AND instr IS 0 AND ch IS "}",
      SET val TO val + "}".
      SET idepth TO idepth - 1.
      SET si TO si + 1.
      CONTINUE.
    /IF.
    IF idepth > 0 AND instr IS 0 AND ch IS "\"",
      SET val TO val + "\"".
      SET instr TO 1.
      SET si TO si + 1.
      CONTINUE.
    /IF.
    IF idepth > 0 AND instr IS 1 AND ch IS "\\",
      SET si TO si + 1.
      IF si < n,
        REAP ch FROM char_at, src, si.
        IF ch IS "n", SET ch TO "\n". /IF.
        IF ch IS "t", SET ch TO "\t". /IF.
        IF ch IS "r", SET ch TO "\r". /IF.
      /IF.
    /IF.
    IF idepth > 0 AND instr IS 1 AND ch IS "\"",
      SET val TO val + "\"".
      SET instr TO 0.
      SET si TO si + 1.
      CONTINUE.
    /IF.
    IF si < n,
      SET val TO val + ch.
      SET si TO si + 1.
    /IF.
  /SEASON.
  IF done AND si < n AND char_at(src, si) IS "\"", SET si TO si + 1. /IF.
  GIVE plant_list_make(3, val, si, hasi).
/ACTION.

# ── skip_comment(src, i, n) ───────────────────────────────
ACTION skip_comment(src(TX), i(NUM), n(NUM)),
  SEASON i < n AND char_at(src, i) ISNT "\n",
    SET i TO i + 1.
  /SEASON.
  GIVE i.
/ACTION.

# ── scan_tokens(source) ───────────────────────────────────
ACTION scan_tokens(src(TX)),
  CREATE tokens(LIST) TO plant_list_make(0).
  CREATE i(NUM) TO 0.
  CREATE n(NUM) TO LEN(src).
  CREATE ch(TX) TO "".
  # v0.49.11 — line_start marks the first token on each line (3rd
  # element of the token pair); parse_field_access uses it to tell a
  # chain dot from a statement-terminator dot followed by a new
  # statement (m.name. / m.put(...) must not merge into one chain)
  CREATE line_start(NUM) TO 1.

  SEASON i < n,
    REAP ch FROM char_at, src, i.

    # Whitespace
    IF ch IS " " OR ch IS "\t" OR ch IS "\r", SET i TO i + 1. /IF.
    IF ch IS " " OR ch IS "\t" OR ch IS "\r", CONTINUE. /IF.

    # Newline
    IF ch IS "\n", SET i TO i + 1. /IF.
    IF ch IS "\n", SET line_start TO 1. /IF.
    IF ch IS "\n", CONTINUE. /IF.

    # Comment
    IF ch IS "#", REAP si FROM skip_comment, src, i + 1, n. /IF.
    IF ch IS "#", SET i TO si. /IF.
    IF ch IS "#", CONTINUE. /IF.

    # any token emitted this iteration opens the line?
    CREATE ls0(NUM) TO line_start.
    SET line_start TO 0.

    # String literal (with ${...} interpolation, v0.48.22-patch2)
    IF ch IS "\"", REAP si FROM match_string_i, src, i, n. /IF.
    IF ch IS "\"", IF si[2] IS 1, PUT plant_list_make(3, "INTERP", si[0], _from_long(ls0)) INTO tokens. /IF. /IF.
    IF ch IS "\"", IF si[2] ISNT 1, PUT plant_list_make(3, "STRING", si[0], _from_long(ls0)) INTO tokens. /IF. /IF.
    IF ch IS "\"", SET i TO si[1]. /IF.
    IF ch IS "\"", CONTINUE. /IF.

    # Single-quoted string literal (v0.49.20) — same STRING token,
    # no ${...} interpolation; \' escapes via the shared scanner.
    IF ch IS "'", REAP sqi FROM match_string, src, i, n. /IF.
    IF ch IS "'", PUT plant_list_make(3, "STRING", sqi[0], _from_long(ls0)) INTO tokens. /IF.
    IF ch IS "'", SET i TO sqi[1]. /IF.
    IF ch IS "'", CONTINUE. /IF.

    # Number
    IF ch GREATER THAN OR EQUAL "0" AND ch LESS THAN OR EQUAL "9", REAP si FROM match_number, src, i, n. /IF.
    IF ch GREATER THAN OR EQUAL "0" AND ch LESS THAN OR EQUAL "9", PUT plant_list_make(3, "NUMBER", si[0], _from_long(ls0)) INTO tokens. /IF.
    IF ch GREATER THAN OR EQUAL "0" AND ch LESS THAN OR EQUAL "9", SET i TO si[1]. /IF.
    IF ch GREATER THAN OR EQUAL "0" AND ch LESS THAN OR EQUAL "9", CONTINUE. /IF.

    # Identifier
    REAP ok FROM is_alpha_start, ch.
    IF ok, REAP si FROM match_ident_or_keyword, src, i, n. /IF.
    IF ok, REAP tok_ty FROM keyword_to_type, si[0]. /IF.
    IF ok, PUT plant_list_make(3, tok_ty, si[0], _from_long(ls0)) INTO tokens. /IF.
    IF ok, SET i TO si[1]. /IF.
    IF ok, CONTINUE. /IF.

    # DOT_DOT
    IF ch IS "." AND i + 1 < n AND char_at(src, i + 1) IS ".",
      PUT plant_list_make(3, "DOT_DOT", "..", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.

    # ARROW
    IF ch IS "-" AND i + 1 < n AND char_at(src, i + 1) IS ">",
      PUT plant_list_make(3, "ARROW", "->", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.

    # Combined comparisons >= <= == !=
    IF ch IS ">" AND i + 1 < n AND char_at(src, i + 1) IS "=",
      PUT plant_list_make(3, "GREATER", ">=", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.
    IF ch IS "<" AND i + 1 < n AND char_at(src, i + 1) IS "=",
      PUT plant_list_make(3, "LESS", "<=", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.
    IF ch IS "=" AND i + 1 < n AND char_at(src, i + 1) IS "=",
      PUT plant_list_make(3, "EQUAL", "==", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.
    IF ch IS "!" AND i + 1 < n AND char_at(src, i + 1) IS "=",
      PUT plant_list_make(3, "BANG", "!=", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.

    # STAR_STAR
    IF ch IS "*" AND i + 1 < n AND char_at(src, i + 1) IS "*",
      PUT plant_list_make(3, "STAR_STAR", "**", _from_long(ls0)) INTO tokens.
      SET i TO i + 2.
      CONTINUE.
    /IF.

    # Single-char
    REAP tok_ty FROM char_type, ch.
    IF tok_ty ISNT "", PUT plant_list_make(3, tok_ty, ch, _from_long(ls0)) INTO tokens. /IF.
    IF tok_ty ISNT "", SET i TO i + 1. /IF.
    IF tok_ty ISNT "", CONTINUE. /IF.

    # Unknown
    PUT plant_list_make(3, "ERROR", ch, _from_long(ls0)) INTO tokens.
    SET i TO i + 1.
  /SEASON.

  PUT plant_list_make(2, "EOF", "") INTO tokens.
  GIVE tokens.
/ACTION.
